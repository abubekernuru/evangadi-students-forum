

const signup = async (req, res) => {
    res.json({msg: "this is a signup route"})

    /* 
    const {username, email, password} = req.body;
    const user = await User.
    */
}

module.exports = {signup}